{
  "name": "bloom-pilates",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:pages": "GITHUB_PAGES=1 vite build",
    "deploy:pages": "GITHUB_PAGES=1 vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}
